apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
  namespace: default
  labels:
    app: gateway-proxy
data:
  # Dynamically updated by CI/CD pipeline
  backend_url: "http://backend-service:8080"
